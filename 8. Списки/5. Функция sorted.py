# sorted — не меняет переданный объект, но при этом возвращает отсортированный список
# list.sort — меняет список, ничего не возвращает (None)

lst = [4, 5, 1, 2, 7, 5, 6]

# lst.sort()
# print(lst)

print(sorted("world"))
